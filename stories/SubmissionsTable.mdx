import {Canvas, Meta} from '@storybook/blocks'
import * as SubmissionsTableStories from '../packages/react-formio/src/components/submissions-table/submissionsTable.stories'

<Meta title="Documentation/SubmissionsTable"/>

<style>
  {`
  .sbdocs-wrapper {
    background: #f7f7f7;
  }
  .sbdocs-wrapper .sbdocs-wrapper  {
    background: #fff;
  }
  `}
</style>

# SubmissionsTable

The submission grid will render a list of submissions and allow clicking on one row to select it.

## Props

| Name   | Type                 | Default | Description                                                                      |
|--------|----------------------|---------|----------------------------------------------------------------------------------|
| `data` | array of submissions | []      | A list of submissions to be rendered in the grid.                                |
| `form` | object               | {}      | The form definition for the submissions. This is used to render the submissions. |

## Example

<Canvas of={SubmissionsTableStories.Sandbox} />
