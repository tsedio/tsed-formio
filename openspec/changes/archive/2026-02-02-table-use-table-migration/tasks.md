## 1. Research

- [x] 1.1 Capture v2 versions of `Table.tsx` and `UseTableProps` (git history) for comparison.
- [x] 1.2 Extract the v3 definitions from `packages/react-formio/src/molecules/table/Table.tsx` and `hooks/useTable.tsx`.

## 2. Migration Doc

- [x] 2.1 Expand `docs/migrations/v2-to-v3.md` with the “Table & \`useTable\` specifics” section covering prop comparisons and before/after snippets.
- [x] 2.2 Add dependency and hook checklist (TanStack imports, filter components, keyboard helpers).

## 3. Storybook Reference

- [x] 3.1 Update the `stories/MigrateToV3.mdx` doc with a Table-specific section that links to the detailed guide and summarizes the key changes.

## 4. Cross-links & Validation

- [x] 4.1 Link the new table migration doc from the main v3 migration guide / README where appropriate.
- [x] 4.2 Proof-read the new docs and ensure Storybook navigation renders the entry without build errors.
