## 1. Research

- [ ] 1.1 Capture v2 versions of `Table.tsx` and `UseTableProps` (git history) for comparison.
- [ ] 1.2 Extract the v3 definitions from `packages/react-formio/src/molecules/table/Table.tsx` and `hooks/useTable.tsx`.

## 2. Migration Doc

- [ ] 2.1 Draft `docs/migrations/table-use-table.md` with overview, prop comparison tables, and before/after code snippets.
- [ ] 2.2 Add dependency and hook checklist (TanStack imports, filter components, keyboard helpers).

## 3. Storybook Reference

- [ ] 3.1 Create or update a Storybook MDX doc (e.g., `stories/TableMigration.mdx`) that links to the detailed guide and summarizes the key changes.

## 4. Cross-links & Validation

- [ ] 4.1 Link the new table migration doc from the main v3 migration guide / README where appropriate.
- [ ] 4.2 Proof-read the new docs and ensure Storybook navigation renders the entry without build errors.
